name: Cleanup previews job

on:
  workflow_call:
    inputs:
      group:
        description: Name of the group to sync (from repo-sync.yml)
        required: true
        type: string
      id:
        description: Identifier of the calling job
        required: true
        type: string

jobs:
  cleanup:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v2

      - name: Load config
        id: config
        run: |
          CONFIG_FILE="repo-sync.yml"

          # Extract repos for the specified group
          REPOS=$(yq eval ".${{ inputs.group }}.repos | .[]" "$CONFIG_FILE")
          echo "repos<<EOF" >> $GITHUB_OUTPUT
          echo "$REPOS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Cleanup stale branches
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_SYNC_TOKEN }}
          REPOSITORIES: ${{ steps.config.outputs.repos }}
          PREVIEW_BRANCH_PREFIX: "ci/repo-sync-preview"
        run: |
          echo "### 🧹 Stale branch cleanup summary" >> $GITHUB_STEP_SUMMARY
          echo >> $GITHUB_STEP_SUMMARY

          # Fetch all open PR numbers from this repo once
          echo "Fetching open PRs from ${GITHUB_REPOSITORY}..."
          OPEN_PR_NUMBERS=$(gh api \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "/repos/${GITHUB_REPOSITORY}/pulls?state=open" \
            --jq ".[].number" 2>&1 || true)

          echo "Open PR numbers fetched (count: $(echo "$OPEN_PR_NUMBERS" | wc -w))"

          # Read repositories into array
          mapfile -t REPOS <<< "$REPOSITORIES"

          echo "Processing ${#REPOS[@]} repositories..."

          for repository in "${REPOS[@]}"; do
            # Skip empty lines
            [[ -z "$repository" ]] && continue

            echo "::group::📂 $repository"

            # Extract repo name (handle optional @branch suffix)
            IFS="@" read -ra REPO_INFO <<< "$repository"
            REPO_NAME="${REPO_INFO[0]}"

            echo "Fetching branches from ${REPO_NAME}..."

            # Get all branches matching the preview pattern
            PREVIEW_BRANCHES=$(gh api \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "/repos/${REPO_NAME}/branches" \
              --jq ".[] | select(.name | startswith(\"${PREVIEW_BRANCH_PREFIX}-\")) | .name" 2>&1 || true)

            FETCH_STATUS=$?
            echo "Branch fetch status: $FETCH_STATUS"

            if [[ -z "$PREVIEW_BRANCHES" ]]; then
              echo "⚪ **${repository}** - No preview branches" >> $GITHUB_STEP_SUMMARY
              echo >> $GITHUB_STEP_SUMMARY
              echo "::endgroup::"
              continue
            fi

            DELETED_BRANCHES=()

            while IFS= read -r branch; do
              [[ -z "$branch" ]] && continue

              # Extract PR number from branch name
              PR_NUMBER="${branch#${PREVIEW_BRANCH_PREFIX}-}"

              # Check if the PR number is in our list of open PRs
              if echo "$OPEN_PR_NUMBERS" | grep -qx "${PR_NUMBER}"; then
                # PR is still open, keep the branch
                continue
              else
                # Delete the branch
                if gh api \
                  --method DELETE \
                  -H "Accept: application/vnd.github+json" \
                  -H "X-GitHub-Api-Version: 2022-11-28" \
                  "/repos/${REPO_NAME}/git/refs/heads/${branch}" 2>/dev/null; then
                  echo "  🗑️  Deleted ${branch} (PR #${PR_NUMBER})"
                  DELETED_BRANCHES+=("${branch}")
                else
                  echo "  ⚠️  Failed to delete ${branch} (PR #${PR_NUMBER})"
                fi
              fi
            done <<< "$PREVIEW_BRANCHES"

            if [[ ${#DELETED_BRANCHES[@]} -gt 0 ]]; then
              echo "🧹 **${repository}** - Deleted ${#DELETED_BRANCHES[@]} stale branch(es)" >> $GITHUB_STEP_SUMMARY
            else
              echo "⚪ **${repository}** - No stale branches" >> $GITHUB_STEP_SUMMARY
            fi
            echo >> $GITHUB_STEP_SUMMARY

            echo "::endgroup::"
          done
